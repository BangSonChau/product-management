mixin alert-success(time)
  - if (messages.success)
    .message.success
      div(
        class="alert alert-success alert-dismissible fade show",
        show-alert
        data-time=time
      ) #{messages.success} <span close-alert></span>