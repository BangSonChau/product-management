extends  ../../layouts/default.pug

block main
  h1 Danh sách sản phẩm

  .card.mb-3
    .card-header Bộ lọc và Tìm kiếm
    .card-body
      .row
        .col-6
          each item in filterStatus 
            button(
              class=`btn btn-sm ml-1 btn-outline-success ${item.class} me-2`
              button-status = item.status
            ) #{item.name}
        .col-6
          form(id="form-search")
            .input-group
              input.form-control(
                type="text"
                placeholder="Nhập từ khóa"
                name="keyword"
              )
              .input-group-append
                button.btn.btn-success(type="submit") Tìm
  
  .card.mb-3
    .card-header Danh sách sản phẩm
    .card-body
      table.table.table-hover
        thead
          tr
            th STT
            th Hình ảnh
            th Tiêu đề
            th Giá
            th Trạng thái
            th Hành động
        tbody
          // Sản phẩm 1
          each item, index in products
            tr
              td #{index+1}
              td
                img.product-img(src=item.thumbnail alt="iPhone 9")
              td #{item.title}
              td #{item.price}$
              td
                if (item.status === "active")
                  button.badge.bg-success Hoạt động
                else
                  button.badge.bg-danger Dừng hoạt động
              td
                button.btn.btn-warning.btn-sm.me-1 Sửa
                button.btn.btn-danger.btn-sm Xóa
